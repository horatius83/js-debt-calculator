import {round} from '../../app/modules/mathutil.mjs'

describe('mathutil', () => {
    describe('round', () => {
        it('should return number if places is 0', () => {

        }),
        it('should return number to 1 places if places is 1', () => {

        })
    })
});